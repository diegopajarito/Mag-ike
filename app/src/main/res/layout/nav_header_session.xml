<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:weightSum="1">

    <ImageView
        android:id="@+id/avatar_icon_header"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_avatar_female_cyclist_1_bw"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:layout_weight="0.80"
        android:layout_width="100dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="@string/avatar_label"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:id="@+id/avatar_name_header" />

    <TextView
        android:id="@+id/avatar_email_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="esr07@geo-c.eu" />

</LinearLayout>
